[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ppsspp-cwcheat-clippy"
dynamic = ["version"]
description = "cwCheat address rewriter for PSP/PPSSPP memory patch lines"
readme = "README.md"
requires-python = ">=3.8"
authors = [{ name = "Tim Abdiukov", email = "TAbdiukov@gmail.com" }]
keywords = ["ppsspp", "psp", "cwcheat", "gaming"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Operating System :: OS Independent",
  "Topic :: Games/Entertainment",
  "Topic :: Utilities",
  "Topic :: Software Development :: Build Tools",
  "Topic :: Software Development :: Disassemblers",
  "Topic :: Security",
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Intended Audience :: Science/Research",
]
dependencies = [
  "pyperclip>=1.9.0,<2"
]

[project.urls]
Homepage = "https://github.com/TAbdiukov/ppsspp-cwcheat-clippy"
Issues = "https://github.com/TAbdiukov/ppsspp-cwcheat-clippy/issues"

[project.scripts]
ppsspp-cwcheat-clippy = "ppsspp_cwcheat_clippy.clippy:main"

[tool.setuptools.dynamic]
version = { attr = "ppsspp_cwcheat_clippy.__version__" }
